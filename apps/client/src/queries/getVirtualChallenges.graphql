query getVirtualChallenges {
  virtualChallenges {
    nodes {
      id
      name
      createdAt: created_at
      checkpoints {
        totalCount
        points {
          lat
          lng
        }
      }
    }
  }
}
